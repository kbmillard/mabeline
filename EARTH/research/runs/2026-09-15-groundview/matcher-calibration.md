# Matcher calibration

`groundview_match_v1` weights, caps, and competition factors are frozen. Field-calibrated score behavior requires `groundview_match_v2` beside v1, never an overwrite of v1 outputs.

Phase 13 software sweeps labelled fixtures. Physical precision/recall is `FIELD_VALIDATION_REQUIRED`.
