# mabeline lab archive (2026-09)

Shelf for non-Vercel lab code moved off `main`.

- `catalog/` — Python grind / geocode / QID / family tooling
- `EARTH/research/...` — GroundView research packs (git-tracked slice)
- `bin/`, `tests/` — catalog CLI + unit tests
- `requirements-catalog.txt`, `requirements-ml.txt` — lab Python deps

**Not here / do not delete elsewhere:**
- Vercel `financial/` → mabeline.vercel.app (lives on `main`)
- SkyView → separate repo, earth deploy
- Angie's Food Truck → https://github.com/kbmillard/angies → https://www.angieskc.com/

Restore example:
`git checkout archive/lab-2026-09 -- catalog`
